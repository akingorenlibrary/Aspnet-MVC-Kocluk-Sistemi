@using Kocluk.Dto;
@model UserFieldUpdateDto
@{
    ViewData["Title"] = "Güncelle";
}

@if (TempData["UpdateFormSuccessMessage"] != null)
{
    <div class="row">
        <div class="col-4 offset-4 mt-3">
            <div class="alert alert-success">
                @TempData["UpdateFormSuccessMessage"]
            </div>
        </div>
    </div>
}

@if (TempData["UpdateFormErrorMessage"] != null)
{
    <div class="row">
        <div class="col-4 offset-4 mt-3">
            <div class="alert alert-danger">
                @TempData["UpdateFormErrorMessage"]
            </div>
        </div>
    </div>
}

<div class="row">
    <div class="col-4 offset-4 mt-3">
        <form method="POST" asp-controller="User" asp-action="ProfileFieldUpdate">
            <div asp-validation-summary="ModelOnly" class="text-danger small"></div>

            <div class="form-group mb-3">
                <label asp-for="bosAlan" class="form-label">Yenisini girin:</label>
                <input asp-for="bosAlan" class="form-control" placeholder="" />
                <span asp-validation-for="bosAlan" class="text-danger small"></span>
            </div>

            <div class="form-group mb-3">
                <input type="hidden" asp-for="bosAlanAdi" />
            </div>

            <div class="form-group d-grid mb-3 mt-3">
                <button type="submit" class="btn btn-primary d-block">Güncelle</button>
            </div>
        </form>
    </div>
</div>